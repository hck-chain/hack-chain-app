{
  "name": "hack-chain-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo for Hack Chain: blockchain-based cybersecurity certification platform.",
  "author": "Hack Chain Team",
  "license": "MIT",
  "workspaces": [
    "frontend",
    "backend",
    "contracts"
  ],
  "scripts": {
    "bootstrap": "npm install --workspaces --include-workspace-root false",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:contracts": "forge build --root ./contracts",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "build:contracts": "forge build --root ./contracts",
    "coverage:contracts": "forge coverage --root ./contracts",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/RicardoMuchacho/hack-chain"
  },
  "dependencies": {
    "@google/design.md": "^0.1.1",
    "@reown/appkit": "^1.8.19",
    "@reown/appkit-adapter-ethers5": "^1.8.19",
    "dom-to-image-more": "^3.7.2",
    "ethers": "^5.7.2",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "react-icons": "^5.5.0",
    "solc": "^0.8.30"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}
