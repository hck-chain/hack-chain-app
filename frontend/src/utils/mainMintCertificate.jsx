import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './mintCertificate.jsx';

ReactDOM.createRoot(document.getElementById('mintCertificate')).render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
)